syntax = "proto3";
package crossfi.crossfichain.mineplexchain;

import "gogoproto/gogo.proto";

option go_package = "github.com/crossfichain/crossfi-node/x/mineplexchain/types";

// Params defines the parameters for the module.
message Params {
  option (gogoproto.goproto_stringer) = false;
  
}
